import { FC } from 'react';

export const RateRestaurantView: FC = () => {
  // Check if user logged in. If not, carry out a login form and then display the rate page.
  return <div>Rate Restaurant View</div>;
};
